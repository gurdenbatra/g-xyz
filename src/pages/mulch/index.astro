---
import { getCollection } from 'astro:content';
import Garden from '@layouts/Garden.astro';
import ScatteredNotes from '../../components/zones/Mulch/ScatteredNotes.astro';

const pieces = await getCollection('mulch');
---

<Garden
  title="The Mulch"
  description="Poems, essays, music, and audio-visual work — the art that grows alongside the rest."
>
  <div class="mulch-wrap page-wrap">

    <header class="mulch-header">
      <h1>The Mulch</h1>
      <!-- DRAFT (your voice) — edit freely; see docs/COPY-REVIEW.md -->
      <p class="mulch-intro">
        The stuff that isn't work. Poems I've been posting since 2013, a couple
        of tracks I make as enu3, and some moving-image experiments. It piles up
        like leaf litter — pick a note and it'll take you to where the piece
        lives.
      </p>
    </header>

    <section class="scatter-section" aria-labelledby="scatter-heading">
      <h2 id="scatter-heading" class="section-heading">All pieces</h2>
      <ScatteredNotes pieces={pieces} />
    </section>

  </div>
</Garden>

<style>
  .mulch-wrap {
    padding-block: var(--space-16);
  }

  .mulch-header {
    margin-bottom: var(--space-16);
    border-bottom: 1px solid var(--ink);
    padding-bottom: var(--space-8);
  }

  .mulch-header h1 {
    font-size: var(--text-3xl);
    font-style: italic;
    margin-top: var(--space-2);
    margin-bottom: var(--space-4);
  }

  .mulch-intro {
    font-size: var(--text-md);
    line-height: 1.65;
    max-width: 560px;
  }

  .section-heading {
    display: block;
    margin-bottom: var(--space-8);
  }

  .scatter-section {
    margin-bottom: var(--space-20);
  }
</style>
